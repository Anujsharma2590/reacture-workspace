{
  "name": "reacture-workspace",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "version": "1.0.0",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.2.1",
    "typescript": "^5.6.3"
  }
}
